# 判断只许 join 或 pass，失败当旁观

参与判断是给未被点名的群成员打的短调用，不是轮次。只在用户于群里发了无 `@` / `@everyone` 的主转录消息时跑；用户点了名或 Bot 发了消息，未点名的人不再判断。结构化结果只有 `join` / `pass`：要问用户必须先下场再 `ask_user`，否则旁观的 Bot 会在主转录里冒出提问。判断超时、坏输出或端点错误也记为 `pass`——v1 没有跳数或人数熔断，失败默认下场、或对每条 Bot 群消息再判断一遍，都会让群消息自我放大成全员开轮。
