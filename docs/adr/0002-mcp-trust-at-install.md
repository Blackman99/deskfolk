# 已配 MCP 不再按参数二次批准

新增或改 MCP 服务器是信任点（必须每次批准，不能 Always allow）。一旦配上，该服务器的 `tools/call` 直接执行，主机不根据参数是否像工作区外路径再套一层批准。MCP 不保证工具效果可从 schema 看出来；启发式拦一部分、放一部分，既拦不严，又和「已配即共享」冲突。真要按路径囚 MCP，另开 roots / jail 故事，不在 v1 矩阵里装。
