# 桌面主栏切成窗格

桌面端主栏从「一次只看一件事」换成一棵可任意纵横分割的窗格树：每块窗格一组标签页，标签可以是一条会话（连同它的预览、流程图，以及在标签里滑出的设置侧栏）、一个终端、日程图或工作区。分隔条可拖；两条分隔条交汇的地方横纵两个方向都能拖。窄屏（≤680px）和手机不变，仍是一次一屏、一层层盖上去。

## 与 [ADR 0011](0011-session-first-three-pane.md) 的关系：补充，不取代

0011 在三种结构里选了会话优先三栏，并明确否决了「工作台轨道」。那次否决的是**拿轨道取代会话列表**——侧栏不再是名册。这次不动那件事：侧栏仍然是名册，窗格默认装的仍然是会话，会话窗格里仍然是转录加底栏输入，桌面默认布局也仍然是三栏，且可以一键重置回去。0011 锁的形状在每一块会话窗格**里面**原样成立；变的只是主栏可以被分开。

写成「取代 0011」会给人删掉会话优先外壳的许可，那正好是反的。

## 布局存本机，不进守护进程，也不进 URL

照仓库已有的惯例，没有例外：侧栏宽度、预览宽度、置顶会话全都在信使的 `localStorage` 里。布局按机器才说得通——27 寸上排好的四块窗格还原到笔记本上是错的，推给配对手机更没有意义。

URL 仍然只描述一个视图：当前窗格正看着哪条会话。布局树塞不进查询串，也不该塞——塞了就等于给「哪块窗格在前面」造了第二个真相来源。旧链接（`?o=trace&k=`、`?o=workspace&w=`、`?p=`）照常能开，落进当前窗格。

坏数据整份丢弃回默认布局，不做局部修补：半修好的布局是「工作台坏了但说不清哪里坏」的来源。

## 几条落在这上面的语义

- **Stop 打当前窗格那一轮。**「你正看着的那一轮」在四条会话同时可见时没有定义，所以换成键盘所在的那块窗格。注意**当前窗格不等于最上面那块**：一块浮在上面的终端不该把 Stop 从你正在打字的聊天上抢走。
- **已读跟着每块可见窗格各自计时。** 现有护栏（贴底、停留时长、窗口真的聚焦）本来就是每个转录实例各跑一份。理由是四条对话就在眼前时，只标一条已读会让另外三条继续顶着未读角标、继续发通知。代价写在这里：在线状态心跳一次只报一条会话，所以可见但不是当前的那条仍可能弹一次横幅；要根治得把心跳扩成一组会话 id，是协议加守护进程的改动，另开一票。
- **会话的状态跟着会话，不跟着键盘。** 草稿、正在回复的那条、待发的附件、建议芯片、正在发送、跳转高亮、历史游标都记在那条会话自己的 `SessionView` 上。runtime 上同名的字段只是转到「当前选中的那条」，窄屏一次一条时没区别；两条并排时从第二块窗格读它就读到第一块的——在一个输入框里打字，另一个也跟着出字。所以会话窗格（`ChatStage` / `Composer`）只读自己那条的 `runtime.sessionView(id)`，发送、停止、继续、批准、回答、翻更早一页都显式带上会话 id。点进另一块窗格也不再算「换了会话」：不清它的回复对象和芯片，不把别的窗格滚回底部。
- **关标签不杀进程。** 终端标签上的 × 原本直接 `DELETE /v1/terminals/:id`，误点一下就把 `pnpm dev` 杀了。现在 × 只是从这块窗格拿走，结束会话是另一个带确认的动作。
- **一个终端标签就是一条会话。** 起初终端标签里还有自己的一排会话标签和「新建」：新开的终端标签先把守护进程的全部会话列出来，挑中一条再绑上；在里面点新建，又把这个标签改绑到新会话上。于是标签套标签，两层都能开关，而且标签一重新挂载就去挑「最新那条还活着的」，切几次所有终端标签都成了同一个 shell。现在新开终端标签就是新起一个 shell，起好、放进会话列表、绑上之后才显示；标签里只有这条会话的路径、停止和结束会话。关掉标签后还跑着的会话（以及手机上开的），在「＋」和空窗格里按名字「接回」。侧栏的终端按钮先找已经开着的终端标签，没有才新起一个。窄屏和手机没有窗格，仍把全部会话收在一页里当标签。会话不在快照里，所以窗一连上就读一遍列表；读到之前自愈不按终端判死活，否则每次重启都把终端标签全收掉。退出或守护进程自己停掉时，还开着的终端进程一起停，但开在哪个目录、屏幕上有什么记在守护进程里；下次启动按原来的 id 重新开一个，所以这块窗格里的终端标签还指着它，分屏不用重排。结束会话才从这份记录里去掉。
- **会话设置和日程图同时只有一块。** 它们由信使独有一份的状态驱动（未保存草稿、哪个危险确认已就绪），第二块会显示第一块的编辑内容。再打开一次是把那一块挪过来，不是造一个对手。
- **设置是会话标签里的侧栏，不是标签。** 起初它是一种标签：在会话顶栏点一下，旁边就多出一个标签页，和它说的那条会话分开摆着，拖走、关掉都不跟会话走。现在它记在会话标签自己的参数里（`side` 为 `settings`，可再带 `botId` 指群里某个 Bot），从对话右侧滑出。下面垫一层遮罩，但只盖这块窗格：标签栏和别的窗格照常能用；点遮罩或 ✕ 收起，回到这条会话不会收起它。也不加大窗格的最小宽度——开一个侧栏不该把旁边的窗格挤开。群里点某个 Bot 就是那个 Bot 的设置，不带回到群设置的返回键；私聊里点它的 Bot 就是这条会话自己的设置（`botId` 记成空），于是顶栏按钮认得出它开着。会话设置仍由信使独有一份的状态驱动，所以全窗只在一条会话上开着，在别处打开会把原来那处收起；草稿按开着设置的那条会话算，不跟键盘所在的窗格走。旧布局里的设置标签按未知种类在自愈时去掉。模型选择记录起初也是这样一种标签，后来并入了流程图的卡片，见 [ADR 0026](0026-model-choice-on-the-flow-board.md)。
- **每条会话一块预览、一块流程图。** 起初按「内容完全相同才复用」：点三个附件就是三块预览标签，从流程图卡片上开文件又是另一块，同一条会话的产物散在一排标签里。现在预览和流程图按会话绑定：会话里的附件和文件链接、预览自己的文件树、流程图卡片上的文件都落进这条会话的那一块预览；打开这条会话的另一件事，流程图那一块转过去；只说「打开流程图」不指定哪件事，就只是把它带到前面，不换它正看着的那件。不同会话各有各的。那块预览里有没保存的编辑时，先由它自己的保存 / 放弃 / 取消来问，答了才转过去——它是这条会话唯一的一块，没有「开在旁边」可选。规则落地前存下的布局可能有重复的，载入时只留离键盘最近的那块（当前窗格优先，再按阅读顺序），其余照关标签的方式关掉。

## N 条会话的历史是刻意串行取的

`EventSync` 的缓冲按连接计，`pause()` / `install()` 是全局的一对，两个并发的会话快照读会互相吃掉对方的帧。所以恢复一个四窗格布局是四次**串行**往返，每块窗格各自显示自己的加载态。这是被 `event-sync.ts` 逼出来的，不是疏忽。

## 一套内容，两个宿主

终端、工作区、流程图各拆成 `XView`（只有内容，填满放它的地方）和 `XOverlay`（窄屏那套壳）。不许出现 `mode` / `phone` 这类 prop——那正是两个宿主开始长出不同行为的方式——并且由 `pane-content-drift.test.ts` 读源码强制执行，不靠自觉。

## 用窗格树而不是全局网格

规范化之后的树有一条性质：每个交汇点恰好有一条连续的分隔条穿过，垂直于它的那些只能抵住它。于是「交汇处两个方向都能拖」是结构上成立的。全局网格能表示四块矩形角对角相碰、没有任何连续分隔条穿过的交点，那里两个方向拖没有定义。

分支是 n 元不是二叉：三栏在二叉树里是 `Row(A, Row(B,C))`，拖 A|B 会让 C 跟着动。

## 相关改动

- `.scratch/terminal/issues/07-desktop-terminal-ui.md`：「整个面板共用一个 xterm 实例」改为每个可见窗格一个；标签归属进持久化布局。该票的「不进 URL」继续成立。
- `overlays/trace-window.ts` 删除：流程图自己那套浮动窗被浮动窗格取代，仓库里只保留一份浮动帧实现。
