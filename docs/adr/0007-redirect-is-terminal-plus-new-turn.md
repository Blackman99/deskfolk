# 改道是终态加新轮，不是同一行接着跑

词表「改道不是另开一轮」指用户侧这个 Bot 仍然只有一轮活着，不是 SQLite 同一行覆盖 `trigger_message_id` 接着跑。`turns.status = redirected` 是终态：旧轮的花费、触发消息、被作废的批准 / 提问留在那一行；同一 Bot 就这条新消息再插入一轮。否则 `redirected` 几乎不会停在表里，触发也无法一对一。分叉才是两行同时进行中。
