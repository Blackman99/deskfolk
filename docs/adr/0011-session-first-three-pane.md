# 信使采用会话优先三栏

三种可点结构里人选了会话优先三栏（侧栏按群 / 你↔Bot / Bot↔Bot 列会话，名册只是顶上一行字母块；线程右抽屉；批准卡和提问都在主转录里）。名册优先（B）和工作台轨道（C）更远，和已锁的「侧栏名册 / 私聊 / 群；中间转录；底栏输入」不合。配色不是这条决定。

后续：[桌面主栏切成窗格](0025-desktop-pane-workbench.md) 在此之上补充了桌面端的分割能力。侧栏名册、会话优先和底栏输入没有变，只是主栏可以分成多块窗格，且能一键重置回三栏。
