# Deskfolk

本机 macOS 上的单人 agent 协作应用。协作形态对齐 Grok Bot：命名持久的队友、私聊、群、点名、异步交接；电脑是本机共享目录，不是云 VM。

> **WIP**：本文定义领域语言，不是所有能力均已完成验收的声明。当前范围见 [README](README.md)；agent 决策、自动任务反馈、逐次反思及完整对话操作覆盖的建设方向见[路线图](ROADMAP.md)。

## Language

**Bot**：
有名字、职责和边界的持久队友。有自己的 1:1 会话与人设；不因一次任务结束而消失。遇到障碍先主动排查、尝试可用办法并验证原始目标，不把可自行完成的技术工作交给用户；只有用户独有的权限、凭据、信息或决策才求助。主动尝试仍受批准、拒绝、Stop 和人设边界约束，不盲目重复有副作用的动作。
_Avoid_: Agent, 子 agent, subagent, 助手, Endpoint

**名册（Roster）**：
这台机器上全部 Bot 的集合。没有人数上限；谁都可以建。Bot 不是安全边界。信使里名册是侧栏顶上一行字母块，不是卡片宫格，也不含你。
_Avoid_: 团队账号, 租户, 名额, 名册卡片, 把你放进名册行

**群（Group）**：
至少两个 Bot 的共享会话（人数无上限），有自己的名字：谁建谁起，不能空，不从在场者拼接。交接要可见时用群，而不是把人当路由器。少于两个 Bot 的对话是私聊，不是群。
_Avoid_: 频道, 房间, workflow, 计划 DAG, 群人数上限, 用成员名单当群名

**私聊（Direct）**：
你与一个 Bot，或 Bot 与 Bot 的一对一会话。Bot 之间可异步私信交接；Bot↔Bot 私聊里一方这一轮的收尾回复和 send_message 一样，会叫醒另一方。谈到静下来，开它的那个 Bot 会被一条回看叫回来源会话交代结果（见「回看」）。你与一个 Bot 的私聊只有一条，一直用下去；Bot↔Bot 私聊是一次发起一条，每条只谈那一件事，不接在上次后面。Bot↔Bot 私聊你只能看，不能发言。
_Avoid_: 线程（thread 留给回复一条消息）, 每对 Bot 一条长期私聊

**会话（Session）**：
私聊或群的对话容器。消息、轮次、判断和花费都挂在它上面；Bot↔Bot 私聊也是会话，你不是参与者，能打开看但发不了言。
_Avoid_: 频道, 房间, conversation（对外不说）, 把线程当会话

**来源（Origin）**：
一条 Bot↔Bot 私聊是被哪条消息开起来的：叫醒发起方那一轮的那条消息。入口挂在那条消息下方，侧栏那一行也标出来源并可点回去。来源会话被清空或删除后，这条私聊就没有来源了，照常能看。
_Avoid_: 父会话, 线程, 把 Bot 自己那句「我去问问 X」当来源

**交接（Handoff）**：
一个 Bot 把活交给另一个 Bot：群里发言或私信。接收方醒来处理，并可稍后回复。
_Avoid_: 派工, 路由, dispatch, bind

**工作区（Workspace）**：
账户级一个本地目录。文件、产物、交接附件都在这里；所有 Bot 共用。Bot 之间不隔离。信使侧栏可打开整棵工作区目录（桌面端是一块窗格：文件树 + 预览，不占用对话框旁那条产物预览；窄屏和手机上仍是从右侧滑出的浮层）；区内 UTF-8 文本可在查看器里改并保存回同一路径。
_Avoid_: 云电脑, 每 Bot 一个目录, sandbox（除非以后另开）

**规划（Plan）**：
一个会话里正在推进的一件事，由应用自己整理出来，不用你先定义。代码里沿用 `tasks` 表和 `task_id`（历史名，注释里叫 plan）。它有**要点**：类别（用来找先例）、目标（现在到底要做什么）、验收（怎么算完成）、规则（你定过的口径、约束、改善意见）、流程与分工、进展（已完成 / 待做 / 卡住）、状态（进行中 / 已完成 / 搁置）；另记着开它的那条要求的原文（最初的要求）。要点由整理跳在你每条消息之后、以及规划静下来之后更新，每改一版留痕（哪条消息或哪一轮引起的，或是你改的），你也能在流程图里直接改。一个会话同时只有一个当前规划：整理跳判成换了一件事就新开一个、原来的搁置，判成回到以前那件就把它接回来，判成做完或搁置就空出来；没有时钟会替你切事。每一轮的局面里都有它的要点、任务清单、本轮任务和同类先例，交接到别的 Bot、别的会话、回看醒来也一样，所以原始目标不靠转录窗里还留不留着那一句。第一轮在局面里标出来，是唯一还能先对齐要求再动手的那一轮。
_Avoid_: 任务表, 计划 DAG, 状态机, 负责人, 调度器分派, 按时钟切事, 让 Bot 自己改规划, 让你先定目标才能开工, 让模型替你摘要目标（要点是整理出来的事实，不是转录摘要）, 把最初的要求改写掉（原文另存）

**任务（Ticket）**：
规划下一个有交付物的最小单位：序号、标题、说明与验收、状态（待做 / 进行中 / 待验收 / 已完成 / 搁置）、谁在做（观察到的记录，不是分派）、产物。只由整理跳和你改：整理跳按 id 引用已有任务、只改变了的字段，新任务用 new-N 开，不能删，只能改成已完成或搁置；你在流程图里改状态。每个任务在规划目录下有自己的文件夹 `NN-<标题>/`，被归到这个任务的轮次默认就在里面干活，没归到任务的轮次落规划目录。产物按任务归属：消息带着 `ticket_id`，它引用的文件就是这个任务的产物，不搬文件。流程图旁边列出任务清单，点一条点亮它的卡片。
_Avoid_: 派工, 认领, 截止日期, 优先级排序, 子任务树, 让 Bot 建或关任务, 按消息一句一个任务, 把镜像文件当任务的事实来源

**整理跳（Organizer）**：
替会话整理规划和任务的一次无工具短调用，跑在默认端点的默认模型上。两个时机：你的消息进会话之后、开轮之前，判这条消息是接着当前规划（continue）、换了一件事（new）还是回到以前那件（resume），更新要点和任务，并标出这条消息说的是哪个任务——消息盖上规划和任务后，它开的每一轮（点名、判断下场、分叉）都落在同一处；规划的活轮全部结束、静下来 30 秒之后再整理一次，把这几轮交出的东西归到任务上、更新任务状态、谁在做和进展。它看的是当前规划的要点和任务、上一版之后发生的事（谁说了什么、交出了什么、经过）、这个会话之前推进过的规划、已有的类别标签和同类先例。失败即不改：没配默认模型、端点拒绝、答得不成形、库拒绝这次改动，规划照旧，轮照开。整理的这几秒里没有轮，但这条消息要叫醒的 Bot（私聊那一位；群里点到的，没点名就是在场的每一位）已经在它下面显示思考中，列表、顶栏里也是；整理完各自交给自己的轮或判断，中间不断档。花费单列一类「整理」，归决策路由，不归任何 Bot。
_Avoid_: 让 Bot 自己整理, 每一跳都整理, 整理失败就拦住不开轮, 用 Bot 的模型整理, 整理跳发言或调工具, 整理跳删任务

**先例（Precedent）**：
同一类别最近三个已完成规划的目标、流程、规则和结局，压成几行进局面，整个名册范围内查。Bot 参与一件事时由此知道这类事以前是怎么定下流程的、有什么规则、怎么算做完，不用你再说一遍；整理跳也拿它当参考。类别由整理跳打标签，能对上已有的就原样用。
_Avoid_: 语义检索, 相似度排序, 跨机器的先例库, 把先例当模板硬套

**镜像文件（Mirror files）**：
规划目录下的 `map.md`（要点 + 任务索引）和每个任务目录下的 `ticket.md`（这个任务的要点，附规划的验收和规则），每次整理或你改过要点之后由守护进程重写。是应用自己的文件：Bot 可以 `read_file` 读，不能改、不能交出，也不算产物；规划目录在第一次整理出要点时就会建。要改内容，在流程图里改要点或任务。
_Avoid_: 手改镜像文件, 把镜像当交付物, 让 Bot 维护它, 用它当唯一事实来源（库才是）

**工作目录（Work dir）**：
一个规划的文件夹，在工作区的 `work/` 下，名字是目标（或开头那条要求）的前 12 码点 + 一小段 id，没有日期（旧的带日期的目录不动）；它下面每个任务有自己的 `NN-<标题>/`。一轮的工作目录是它所在的任务目录，没归到任务就是规划目录；壳默认在里面跑，文件工具的路径仍相对工作区根。谁继承：交接、点名、判断下场、Bot↔Bot 私聊、中断续跑和回看都用唤醒方那一轮的规划和任务，所以一件事跨 Bot、跨会话仍是同一处；你的消息由整理跳盖上规划和任务，它开的轮都落在那里。不按时钟切目录：一个会话的当前规划一直开着，直到整理跳判成换事、回到旧事、做完或搁置。日程有自己的常驻规划，每次到点新开一个任务。第一次真被用到才创建，只说话的轮次不留空壳。交付物不进来：你点名了路径就写在那儿，没点名的落本轮任务目录。两层都有保留子目录：`tool-results/` 是守护进程存的大工具结果（关闭够久后自动清掉），`scratch/` 是纯过程文件——都不会挂成产物；`map.md` 和 `ticket.md` 是应用的镜像文件，同样不算产物。删掉工作目录是你的事，应用只清 `tool-results/`。这件事在信使里按叫醒关系从上到下看经过，卡片上标着它干的是哪个任务，产物挂在交出它的那一轮上，点开走预览。
_Avoid_: 每 Bot 一个目录, 产物库, 把它当沙箱, 应用替你删整个目录, 应用搬文件归档, 按日期或静默时长切目录

**批准（Approval）**：
危险动作在执行前必须由你放行。批准管的是「接下来要做的那一下」，不能撤销已经做完的。
_Avoid_: ConsentGate, 闸门（openbot 词）

**日程（Routine）**：
挂在某个 Bot 上、按日历触发的重复工作。v1 只有日程，没有事件触发。每条日程有一个常驻规划（不占会话的当前规划，不经整理跳），每次到点在它下面新开一个任务 `NN-<日期>`，那一轮就在这个任务目录里干活。到点时守护进程在你↔该 Bot 的私聊插一条该 Bot 名下的系统行「日程「<名字>」：<要做的事>」叫醒它，和回看一样，不以你的名义发消息。回看不是日程，见「回看」。
_Avoid_: cron 作业（对外不说）, skill 包自动化, 以你的名义发出日程内容

**回看（Check-back）**：
Bot 给自己约的一次性叫醒：多少分钟后（一分钟到七天）、回看时核对什么、没动静怎么办。到点后守护进程在同一个会话插一条「回看：<备注>」的系统行叫醒这个 Bot。这一行是它给自己的提醒，只有被叫醒的那一轮读得到、流程图画出它叫醒自己；会话里不显示，别的 Bot、整理跳、搜索和未读都看不到，就像人不会把自己的备忘念给大家听。那一轮沿用约它那一轮的规划和任务，局面里有这件事的要点、任务清单、已交出的文件和经过；群里它已有活轮就听进那一轮，私聊里另开一轮。用在交接出去之后、或在等一个不由自己掌控的结果时，让交出去的活有人回头核对，而不是靠对方记得点名回来。一个 Bot 在一个会话里同时只有一次待回看，再约就是替换；局面块会标出自己待回看的那一次。约它的那一轮被 Stop、会话清空或删除、Bot 删除都作废；Bot 离开了群或被归档，到点只消掉不叫醒。守护进程睡过去或重启后，到期未响的补响一次。不进日程图，不是事件触发。应用也会替 Bot 约一次：它开的 Bot↔Bot 私聊静下来（最后一轮结束约 10 秒后，里面没有活轮也没有待回看），就在开这条私聊的那个会话立刻叫醒它，回看行里写着和谁谈的、最后一句是谁说的什么（对方一句没回就说没回），让它先在那里交代结果再推进；这同样替换它在那里原来的待回看。私聊里自上次叫回以来没有新的话就不再叫；从这种回看开出去的私聊，对方一句没回也不再叫回，免得两个 Bot 对着沉默来回空转。私聊里的轮被 Stop 或因重启中断时不叫回，那是你要接手的事。
_Avoid_: 轮询, 每几分钟一次的自我叫醒, 进日程图, 事件触发, 中央调度替 Bot 决定回看, 一个会话里叠几次回看

**收尾自检（Closing check）**：
一轮要把交付物交给你之前的一次核对：Bot 准备发出的消息引用了工作区文件、目标会话里有你、这件事有要点或最初的要求，守护进程就在发出前打一次无工具短调用（默认端点的默认模型），拿本轮任务的要点、规划的验收和规则（还没整理出要点时用最初的要求）对照这件事已交出的文件、谁做了什么和这条收尾消息，列出既没有交出、收尾里也没有交代去向的要求。有就退回给 Bot 一次：走 send_message 的，那次调用不执行，结果是 `closing_check` 和清单；直接回复的，清单作为一行提示进回路。Bot 补上，或在收尾里说明交给谁、为什么不交、什么时候做，再发一次就过。一轮只核一次，第二次不再拦。是带依据的提醒，不是闸门：既没有要点也没有最初的要求、没配默认模型、端点拒绝、答得不成形都直接放行。退回不算工具错误，复盘不把它当失败；这次调用记在这一轮的花费上。发到没有你的 Bot↔Bot 私聊不核，没引用文件的回复不核。
_Avoid_: 每跳都核, 拦住不让发, 把退回算成工具错误, 用 Bot 自己的模型核自己, 让模型改写收尾

**技能（Skill）**：
挂在某个 Bot 上的一条具名工序：短说明（何时用）+ Markdown 正文（怎么干）。只属于该 Bot，不是名册共享，不是工作区文件，也不是 MCP。启用中的技能以目录形式进入该 Bot 每一跳的 system；正文按需用工具读进本轮回路。技能是工序，MCP 是能力：Bot 先看技能目录，命中就读正文照做，正文点到的 MCP 工具按名调用；没有命中的技能才直接从「本轮 MCP」段挑工具。技能不新增工具，也不替代 MCP。技能可声明正文依赖的 MCP 服务器名；本轮未连接的会在目录里标出，正文照做不了时直说或提问，不拿别的工具凑。读正文时，正文里写的 `mcp_` 工具名若不在本轮工具里会被标出来，Bot 按「本轮 MCP」段找现名并改正文。Bot 可用工具增删改自己的技能，你也可在人设里改；改完不插转录条。技能不能取消批准、不能新增工具、不能把工作区外当成区内。
_Avoid_: skill 包, `/skill`, plugin, 工作流, 把人设当工序, 名册级共享技能, 按关键词自动塞正文

**记忆（Memory）**：
某个 Bot 自己记下的一条跨会话仍然成立的事实：一个主题 + 一句结论。只属于该 Bot，别的 Bot 看不到，删除 Bot 一并删掉，归档保留。Bot 用工具自己写，链结束之后的学习跳也可以写一条：复盘归咎模型、踩过工具错误但做完了，或需求没说清且用户纠正了不止一轮（这种只记用户补上的口径——交付形态、范围、给谁看、默认值——不改技能）；不调用就不留，不是后台替它蒸馏，也不是把转录摘要进人设。启用中的全部记忆进入该 Bot 每一跳的 system，没有检索、没有相关度排序。记忆是以前的结论，不是事实来源：与本轮转录冲突时以转录为准，不再成立就改掉或删掉。同一个主题再记一次是覆盖，不并列第二条。条数有上限，满了要 Bot 自己先取舍，不静默挤掉。你能看、能改、能停用、能删，但不替 Bot 新建；每条都带记下它的那条消息，可跳回去。三者分工：人设是你是谁，技能是怎么做，记忆是你学到了什么。
_Avoid_: 检索式记忆, 向量库, 原始转录回灌, 后台蒸馏器, 把记忆当事实来源, 跨 Bot 共享记忆, 用户手动新建记忆, 无上限的记忆目录

**守护进程（Daemon）**：
本机常驻的 TypeScript 进程，自己跑 tool-call 循环。关窗不停；窗口崩了轮次还在。插电时挡住空闲睡眠；合盖、菜单栏睡眠、没电、关机仍会睡，唤醒后续上。只有窗口进程还活着（含藏到托盘）时，被杀的守护进程会被拉起来；窗也没了之后再崩就停，直到你再开 App 或下次登录。设置里有可选独立运行时，**默认关**，生产在原生封闭门通过前保持禁用；未开启时监督仍是窗口进程。它还持有你开的终端会话，所以关窗之后那些进程照跑。退出或守护进程自己停掉时，进程一起停，但开在哪、屏幕上有什么会留下来；下次启动在原来的位置重新开一个，分屏布局按这个接回去。结束会话才忘掉它。
_Avoid_: 云电脑, 24/7 服务器, 把 UI 进程当运行时, 合盖仍继续, 杀一次就停, 独立 KeepAlive 登录项, 关窗杀掉终端

**窗口（App window）**：
Tauri 桌面窗，里面是 Svelte 信使。关窗是隐藏到托盘，不是退出；隐藏时仍留在 Dock。登录时只到 Dock 和托盘，窗不弹。点 Dock / Cmd-Tab / Spotlight / 托盘左键 / 再开 App 把窗叫回来。同时只跑一个窗口进程；第二次启动只唤醒已有的那个并连上已有守护进程。窗口崩了不会自己回来。连上守护进程之前：退出可用，Stop 不可用，信使是「连不上运行时」。拖过的宽高和是否最大化，退出后再开还是关前那样；不记位置，免得换显示器落到屏外。拖边框的过程中不去问窗口是不是最大化：那一问在 macOS 上会把窗口挪开，松手时位置就和拖着的时候不一样。
_Avoid_: 只用浏览器打开, 无托盘, 隐藏即从 Dock 消失, 窗口 KeepAlive, 登录抢屏, 多窗口, 窗崩当退出

**窗格（Pane）**：
桌面窗里的一块内容区。一块窗格是一组标签页；一个标签可以是一条会话（它的产物预览、流程图都跟着它；它自己的设置从这个标签里的对话右侧滑出、带一层只盖这块窗格的遮罩，不另占标签），也可以是一个终端（一个标签一条 shell 会话）、日程图、工作区或花费。窗里可以纵横任意分割出多块窗格，分隔条和交汇处都能拖；标签可以在自己的标签栏里拖动排序、在窗格之间拖、拖到边缘就地分割、按住 ⌥ 拖出来变成浮在上面的窗格。在窗格里任意位置右键，可以向上、下、左、右就地分出一块空窗格；里面本来有右键菜单的（消息、代码编辑器）和输入框仍是它们自己的，按住 ⌥ 右键是网页视图的原生菜单；浮动窗格不就地分，右键提供停靠回布局与关闭窗格。空窗格右上角可直接关闭；每块窗格的右键菜单也可关闭整块窗格及其标签，工作区和产物预览有未保存编辑时先确认。最后一块平铺窗格关闭后留下一块空窗格。**窗里分成多块窗格，那仍然是同一个窗口进程**，浮起来的也不是第二个系统窗口。同时只有一块是当前窗格——键盘在哪块就是哪块，在哪块里点一下或滚一下也换到哪块：Stop 打它那一轮，点通知先找已经开着那条会话的窗格。摆着不止一块时，当前那块四周描一圈强调色，一眼看得出是哪块；只有一块时不描。侧栏里点一条会话：只有一块窗格时，它替换这块窗格正看着的那条会话（终端、工作区这些不被顶掉，新会话在旁边开标签）；摆着不止一块时，已经开着它的那块切到前面，哪块都没开就在当前窗格里新开一个标签，不替换任何一块正看着的东西。会话设置同时只在一条会话上开着，在别处打开就把原来那处收起；日程图同时只有一块。每条会话只有一块产物预览、一块流程图：在这条会话里、它的预览文件树里或它的流程图上再打开别的文件、别的那件事，是那一块转过去，不另开一块。这两块的标签名是固定的，跟会话标题走：「<会话名>流程」「<会话名>的产物」，不跟打开的文件或这件事改。会话窗格不再另画顶栏：头像（带状态点）和名字始终在标签上，顶栏的第二行成了标签的提示；鼠标移到标签上才出现 ⋯，里面是置顶、经过和设置，右键会话标签也在分割前面给出这几项。右键任何一个标签（会话标签的 ⋯ 里也有），可以关闭这个标签、同一块窗格里的其他标签、它右侧的标签或全部标签；全部关掉就和关掉最后一个标签一样，这块窗格跟着关掉，工作区和产物预览有未保存编辑时先确认。其他标签（终端、工作区、日程、花费、流程、产物）也始终在名字前带上各自的图标。窄屏（≤680px）和手机上没有窗格，仍然是一次一屏、一层层盖上去。
_Avoid_: 多窗口, 第二个窗口进程, 把浮动窗格当系统窗口, 手机上分屏, 看得见就算已读, 通知自己改布局, 空窗格留着, 设置另开标签, 窄窗格里顶栏和标签各报一遍名字

**布局（Layout）**：
窗格是怎么分的：纵横分割、各自的宽高、哪些浮着、哪块是当前的、终端窗格摆着哪条会话。只记在这台机器的 localStorage 里，不进守护进程，也不进 URL——URL 只说当前窗格正看着哪条会话。退出再开还是关前那样；引用的会话或终端没了就自己收拾掉；重置回默认的一块窗格前先问未保存的改动。
_Avoid_: 布局进库, 布局进 URL, 跨设备同步布局, 换台机器跟着走, 重置直接丢掉未保存的改动

**退出（Quit）**：
结束这次会话。窗口监督（默认）时窗口进程和守护进程都停，托盘和 Dock 都消失。独立运行时若已开启，退出只关 UI，作业继续；要停作业须显式 stop（写 `runtime.stop`）。进行中的轮次标成中断，你开的终端会话里的进程一起停；下次打开时，每个还开着的终端在原来的位置重新开一个，分屏布局按这个接回去。不是关窗。结束会话才忘掉它。不动登录启动偏好。关机和注销在轮次上也按退出处理。开发态同一条规则。生产独立运行时仍 gated，默认仍是窗口监督。
_Avoid_: 关窗当退出, 默认只关窗留无头守护进程, 退出当关掉开机启动, 开发态 Cmd+Q 只关窗, 把 gated 独立模式当成已开启

**登录启动（Launch at login）**：
可选，默认开。登录时只拉窗口进程到 Dock 和托盘，信使窗不弹；窗口进程再确保守护进程在跑。关掉只影响下一次登录，不杀当前这次；下次登录窗和守护进程都不来。退出也不清这个开关。守护进程不是自己的登录项。
_Avoid_: 登录弹窗, 关掉立刻杀掉当前这次, 守护进程自己当登录项, 退出清掉开机启动, 关掉开机启动却留下会自己起来的作业

**托盘（Tray）**：
窗口进程上的菜单栏图标。左键把窗叫回来。右键：显示窗口、Stop（没开窗则最近活动轮）、退出。没有隐藏、没有暂停守护进程。窗崩了托盘也没了。
_Avoid_: 托盘角标, 第二套信使, 守护进程自己做托盘

**用户**：
跑这台机器的那一个人。没有多用户、没有分享链接。每个群里都有你；你和群里任何 Bot 都能拉人、移出。移出不删 Bot。你出现在会话、转录和在场名单里，不出现在名册那一行字母块上。
_Avoid_: 成员, 租户, 组织, 没有你的纯 Bot 群, 名册里的你

**端点（Model endpoint）**：
一个 OpenAI 兼容的 HTTP 接口（base URL + API key）。应用不自带模型。可以配置多个端点；其中一个是默认。补全打到模型所属的那个端点。你在设置里加，Bot 也能用工具加。新建或改已有 URL 是危险动作。默认端点不能改 URL / 密钥 / 删除。
_Avoid_: 供应商目录, Binder, 晚绑定, 每 Bot 一套端点

**模型（Model）**：
某个端点认的一个补全名字。每个端点有自己的名单和默认，名单上的名字可带选路参考价、可选计费单价、支持的思考等级和擅长领域。选路参考价是应用挑模型时用的相对数；计费单价是 USD / 百万 token，输入和输出一起配，缓存输入可单配，留空沿用输入价。计费单价用于缺实报金额的花费估算；探测模型名单不会自动填单价。用户发消息开一轮时，应用只在 Bot 钉的端点（没钉端点就是默认端点）的名单里选模型和思考等级，不会因为设置里多了一个端点就把它的模型算进别的 Bot；Bot 钉了仍在名单上的名字则模型名受约束，钉的名字只在别的端点上时照钉的用。Bot 也可以钉思考等级（补全的 `reasoning_effort`）：档名以该模型名单为准（常见 none / low / medium / high，端点广告了还会有 xhigh、max 等）。探测 `/models` 时若对象带了思考档就自动写进该模型的 `thinking_levels`，设置里仍可手改。思考等级和模型一起钉，不单独存在：要么两个都不钉、模型和等级都由应用每条消息挑，要么钉一个模型并同时定下它的等级。钉模型时没给等级就落到该模型的默认档（应用对普通消息的偏好顺序）；换成不支持原档的模型会落到新模型的默认档；清掉模型一并清掉等级；没钉模型却给等级会被拒。你在 Bot 面板里点一下就能改，Bot 也能用工具改自己的。空钉不是「永远用端点默认模型」，而是应用可从该端点名单里挑；要用别的端点必须显式钉端点。谁来挑：每开一轮先由 agent 挑一次，它看这条消息、这个 Bot 的人设、候选名单、这个 Bot 最近的复盘结论（带当时那条消息和类别，对不上就当没看见）和最近干净做完的例子，答出模型、思考等级和一句理由；它自己固定跑在默认端点的默认模型上，答不出或答了不存在的东西就落回规则兜底，不让用户卡在选路上。用户后续说的每一句都原样留着（不按关键词筛），落在被引用的那一轮、`@` 的那个 Bot 最近的一轮，或会话里最近可见的一轮上；连着围绕同一件事的若干轮算一条纠正链。链结束时（换了话题或静默一段时间）复盘一次；没有后续发言，但这一轮是端点拒绝、回复不完整或工具错误不少于两次，也复盘一次。复盘看的是整段加上这一轮实际做了什么：这到底是模型不行、事情本身难、还是需求没说清，只有判成模型不行才留成这个 Bot 的经验，并说明该往更强 / 更轻 / 更快 / 更便宜哪个方向调。留下的是结论不是分数，没有权重、抵扣和封顶；被照做两次仍没有更干净的结论退出选路，记录还在。经验归 Bot 自己，删除 Bot 一并删掉。每轮选了什么、怎么结束、做了多少、收到哪些反馈都留有记录。用户不每轮自选。判断调用和轮次补全用同一个解析结果。
_Avoid_: 供应商目录, 判断用另一个名字, 每轮自选, 思考等级永远只由应用选, 不钉模型只钉思考等级, 钉了模型却不定思考等级, 名册级共享的路由经验, 用关键词认模型反馈, 一句话就把一个模型永久钉死, 把选模型做成打分公式

**向导**：
设置的空态：工作区路径、端点 URL、已有密钥齐了即完成。没有单独路由；滑出只盖侧栏，不抢主转录。
_Avoid_: /setup, 全屏 onboarding, 把设置做成主转录

**MCP**：
名册级服务器：stdio（command / args）或 HTTP / Streamable HTTP（url + 可选 headers）。所有 Bot 都能调已配工具。你在设置里加，Bot 也能用工具增删改。新增或改 command·args / url / headers 是危险动作。HTTP 的 Authorization 在批准卡上贴，进钥匙串，不进工具参数。删除 MCP 不是危险动作。应用记下服务器自带的用法说明；你或 Bot 还可给每台服务器写一条用法备注（做什么、何时用、何时不用），进每一跳的「本轮 MCP」段并排在服务器自带说明前面，服务器说明够用时留空即可。改备注不是危险动作，改连接也不清掉它。已启用且连接成功的服务器全部工具对所有 Bot、私聊、群和日程持续可用，不按消息关键词或语言筛掉。新增、修改或重新启用后，当前轮次下一跳及其他 Bot 的后续补全即可使用；停用或删除后不可再调用。要把工作区里的图片交给只收图片 URL 或 data URI 的参数，Bot 在参数里写 `workspace://<相对工作区根的路径>`，应用发出前换成这张图的 data URI（超过半兆先缩小），转录和下一跳里仍是这条短引用；远端服务器读不到本机磁盘，模型也抄不出整张图的 base64。
_Avoid_: 每 Bot 一套 MCP, 把 Bot 当工具权限边界, 按关键词隐藏已配能力, 只在安装当轮可用, 只认 stdio, 手改服务器自带的 instructions

**参与（Participation）**：
群主转录来了一条你或 Bot 的消息时：被点名的必须下场。你的普通消息（无 `@` / `@everyone`）才让其余在场 Bot 各自做一次判断（下场或旁观）。你点了名，或 Bot 发了消息，未点名的人不再判断。应用不代判谁说话，也不用跳数或人数熔断去掐循环。要问你，是下场之后那一轮里的提问，不是判断的第三种结果。
_Avoid_: 中央裁决, Dispatcher（openbot 词）, 路由, 防循环上限, 判断阶段求助, 每条 Bot 群消息都再判断一遍

**判断（Judgement）**：
只在你于群里发了无点名的主转录消息时，给未被点名的群成员打的一次短调用，无工具，结构化返回下场或旁观。判断不是轮次；旁观不进主转录；调用失败当作旁观。点名（含 Bot 点名）跳过判断，只开被点名的轮。判断看得到这个群正在做的那件事：规划的目标、验收、规则、任务清单和各自谁在做、这条消息被归到的任务、已交出的文件和经过；判的是任务里属于自己职责的部分有没有人在做，不只是这一句像不像自己的事。
_Avoid_: 让每个旁观者都开一轮, 判断里提问, 失败默认下场, 对 Bot 消息再判断, 只看这一句不看这件事

**点名（Mention）**：
用 `@Bot` 指定必须下场的队友（跳过判断）；`@everyone` 让当前群里除作者外每个 Bot 必须下场，不把群外的人拉进来。未点名则走参与自判。群里该 Bot 已有进行中的轮，再被点名就听进那一轮，不再分身。名字写成在场成员唯一可辨的缩写（如把「分镜师」写成「分镜」）时按那位成员处理；完全对不上则不叫醒任何人，`send_message` 会先拒绝一次让 Bot 改正，群里最终留一条系统提示。
_Avoid_: 指派, 派工, 群里点名再克隆一个自己

**线程（Thread）**：
针对某一条主转录消息的引用回复：`parent_id` 指向被引的那一条，回复仍留在主转录。点回复会自动 `@` 原发送者（原发送者是 Bot 时）；已有点名或 `@everyone` 则不重复。只能一层，不能再引用一条回复。Bot 用 `send_message` 的 `parent_id` 同一套。
_Avoid_: 把线程当群（群是独立会话）, 引用回复藏进侧栏, 引用用户时自动 @user

**回应（Reaction）**：
对一条消息的轻量确认，从固定表情集合里挑，不能自定义。不承担改指令或安全相关决定；那些必须写字回复或点批准。v1 只有你会贴；Bot 不回应。
_Avoid_: 用表情当批准, 自定义表情, 用 ✅/❌ 当允许或拒绝

**Always allow**：
名册级的一条已保存规则（种类键 + 范围）：同类危险动作以后直接放行。和「允许一次」并存。有的种类是必须批准，不能写成 Always allow。
_Avoid_: 一律放行, bypass, 每 Bot 一条规则

**搜索（Search）**：
跨 Bot、群、文件和日程的查找，跳回原会话位置。v1 做本机检索，不是另一个 Bot。
_Avoid_: 语义记忆层

**Stop**：
你发出的立即停。只作用于私聊里进行中的轮次；已做完的动作不回滚。UI 开着时打在**当前窗格**的那一轮（须是私聊）——同时摆着几条会话时，当前窗格就是键盘所在、或刚在里面点过滚过的那块；菜单栏在没开 UI 时打在最近有活动的私聊里的那一轮。群聊没有 Stop：发出去的群任务不能用按钮打断，要停就发消息让 Bot 们停下来。清空历史、归档、删除群仍会结束该会话的活轮。没有跳数、名册或群人数熔断。
_Avoid_: 同时停掉所有窗格里的轮次, 按最上面那块浮动窗格判定

**轮次（Turn）**：
一个 Bot 被一条消息、点名、交接或日程叫醒，处理到回复、提问或批准卡片为止。补全失败也结束这一轮，终态仍是完成，不是中断。群里同一 Bot 同时最多一轮进行中；再被点名或判断下场时听进那一轮。私聊、日程和你显式「另开一轮」仍可同时多轮。转录按时间交错。花费逐次调用记账，信使有独立的花费视图，会话里不放数字，也不据此停机。一轮一直调工具不交结果时，每满 40 跳在循环里问它一句是不是卡住了（卡住就不调工具、直接回复卡在哪、需要什么；在推进就接着做）；到 160 跳收起工具，下一条回复就是这一轮的结尾。
_Avoid_: 工作流步骤, Planner 步骤, 群里同一 Bot 分身多轮, 把补全失败写成第五种终态, 让一轮无限调工具

**归档（Archive）**：
从名册主列表拿掉，会话、人设和日程仍在，可恢复。删除才从名册去掉这个 Bot（共享工作区文件不动）。
_Avoid_: 隐藏即暂停（归档不等于 Stop）

**危险动作（Dangerous action）**：
执行前必须批准：工作区外读或改、出站网络（模型调用除外）、新增或改 MCP（含 command·args / HTTP url·headers）、新建端点或改已有端点 URL、无约束壳。工作区内读 / 写 / 删、已配 MCP 的调用、工作区壳、往已有端点名单加或改模型名直接干。删除 MCP 不是危险动作。默认端点不能改 URL / 密钥 / 删除。
_Avoid_: 闸门, ConsentGate

**工作区外（Outside the workspace）**：
路径 `realpath` 之后不落在工作区根前缀下。工作区内的 symlink 跟目标走：目标在区外就是区外。
_Avoid_: 只做字符串前缀, 按 inode / 硬链接判定

**工作区壳（Workspace shell）**：
一次通过启动前路径检查的 `shell`：cwd 在工作区内，命令里看得见的路径也不出区。直接干。不是内核囚笼。
_Avoid_: 整机 shell, sandbox, seatbelt, 把 App Sandbox 容器当工作区根

**无约束壳（Unconstrained shell）**：
一次被判定越界的 `shell` spawn。这次进程及其子孙不再走工作区路径检查；下一次 `shell` 重新判定。
_Avoid_: 永久解除, 内核 jail 解除

**终端（Terminal）**：
你自己在这台机器上开的一条 shell 会话，守护进程持有。和 Terminal.app 同权：cwd 默认落在工作区，但不关在工作区里，也不走批准——批准管的是 Bot 的危险动作，你不需要向自己申请。用来跑你自己的活，包括启动项目这类一直开着的进程。关窗不停。它在哪个目录，以 shell 自己报告的为准（zsh 开箱即用，每个提示符前报一次）：`cd` 之后标签名跟着变；从没报告过的，就是开的那个目录。退出或守护进程自己停掉时，里面的进程一起停，但它在哪个目录、屏幕上有什么会留下来，下次启动在那个目录重新开一个。结束会话才忘掉它。屏幕也在守护进程那里：接回时拿到的是它现在的样子，全屏程序也原样；程序问终端的问题只由它回答，窗格不答，所以几个窗格同时看一条会话也只答一次。shell 拿到的环境按 Terminal.app 那样配，但只放行一份白名单，守护进程自己的其他环境变量（包括密钥）不带进去。桌面和手机同时看一条会话时，它的行列按你最近在看、在敲的那一端来。配对过的设备同权，手机上看得见也敲得动。Bot 看不见、用不了，也不知道它存在。桌面上一个终端标签就是一条会话：新开一个终端标签就是新起一个 shell，标签里不再有一排会话可切。关掉标签只是从窗里拿走，进程照跑；没被任何标签摆出来的会话在「＋」和空窗格里可以「接回」。手机和窄屏上没有窗格，所有会话收在终端那一页里当它的标签。要停必须显式「结束会话」并确认。守护进程那份会话列表才是真的；同一个文件夹里开的几条按开的先后编号，桌面标签和手机上叫法一样。
_Avoid_: 把它当工作区壳, 给 Bot 开终端, 让终端命令走批准卡, 关窗即杀, 关标签即杀进程, 把窗格标签当会话列表, 一个终端标签里再套一排会话, 两个标签指着同一个 shell, 跨守护进程重启续命, 云电脑

**命令流（Command stream）**：
Bot 执行一次 `shell` 时 stdout / stderr 的实时外推，让那一次调用在它跑的时候就看得见。只是可见性，不改变那次调用是什么：仍然 stdin 关闭、仍然十分钟超时、路径仍然逐次判定。不是终端，没有输入，不进主转录：进行中挂在那条气泡下面，结束折叠成一行，回看走工作目录的轮次视图。
_Avoid_: 给命令流加输入, 把它当终端会话, 让它改批准粒度, 进主转录, 新增一种消息种类

**提问（Ask）**：
Bot 向你问一件需要判断的事（不是工具批准）。问句出现在转录里，你回答后该轮继续。能列出可能的答案时 Bot 给出选项：只能成立一个是单选，几项能同时成立是多选，推荐的排第一个；你总能不选、自己写，也能选了再补一句。你的回答记在这条提问上，不另发成你的一条消息：卡片上留着你选了什么、写了什么，之后的轮次、整理跳和搜索都从这里读到。一次提问只答一次。
_Avoid_: 把提问做成批准卡片, 把回答另发成一条你的消息, 加「其他」选项, 答完再改答案

**中断（Interrupted）**：
守护进程挂了、被杀后拉起、你退出、关机或注销时，进行中的轮次被标成中断。转录里那一行和其他 Bot 消息一样带头像和名字，「继续」贴在「中断」气泡后面：新开一轮、触发条是这条中断行，不重试断掉的那一下；该 Bot 的下一轮（哪个会话都行）能看见「上次断了」，看一次即止。合盖 / 系统睡眠不是中断：冻住，醒来接着跑。睡着的时间不算卡住，也不算命令超时；被睡眠打断的请求醒来后等网络回来（约一分钟）再整个重发。窗口崩了不是中断：守护进程继续。补全超时、流挂、端点 4xx/5xx 不是中断。健康检查暂时失败而守护进程进程还在，也不是中断：窗不拉第二份，第二份抢不到端口就不改库。
_Avoid_: 从断点接着调工具, 睡眠当中断, 关机当睡眠, 窗崩当中断, 把「上次断了」永远挂在人设上, 把补全超时当中断, 把端点错误当中断, 把健康检查超时当中断

**补全（Completion）**：
一轮里对端点的一次 HTTP Chat Completions（流式）。一轮的工具回路可以有多次补全。补全失败不是中断，也不回放历史工具。
_Avoid_: 把一次补全当成一轮, 把补全超时当中断

**改道（Redirect）**：
在已有进行中的轮次上，放弃未完成的后续动作（状态变为 redirected）并就新消息再开一轮。群里该 Bot 已有活轮时，点名和判断下场走改道，像一个人转头听这一句。私聊里需要立刻停掉进行中的轮次时用 Stop；群聊没有 Stop，要停就发消息。
_Avoid_: 强制要求用户每轮手动选择改道还是分叉, 同一行覆盖 trigger 接着跑, 用户一条消息清掉全群所有活轮

**分叉（Fork）**：
在已有进行中的轮次之外再开一轮，旧轮继续。私聊用户新消息默认分叉（显式才改道）；日程在你↔该 Bot 私聊分叉。群里该 Bot 已有活轮时，点名和判断下场不另开分身；你在群里显式「另开一轮」才分叉。Bot↔Bot 私聊里没有用户，对方第二条消息走改道而不是分叉，和群里对 Bot 的做法一致。
_Avoid_: 人工介入调度开关, 日程改道群里的轮, 群点名再克隆一个自己

**拉入（Pull-in）**：
在群里 @ 一个不在群里、名册上已有且未删除的 Bot：先加入该群再下场。名册没有的名字不新建 Bot，消息仍发出。
_Avoid_: @ 了却不进群, 未知名隐式建 Bot

**产物（Artifact）**：
一条消息里引用的工作区相对路径，或 `http`/`https` 链接。文件本体在工作区（或远端 URL）上。本轮写入的区内文件会自动出现在最终消息里。点开后在聊天右侧预览面板查看（图 / 音视频 / PDF / 文本 / 单文件 HTML / Word / Excel / PowerPoint）；其它种类（字幕、压缩包……）写明不支持预览：本机窗口给「用系统默认应用打开」，手机和浏览器里给下载按钮。远程打开的每个文件都能下载：手机在预览顶栏右端，宽屏在文件上方那排按钮里，流程图的文件卡片在卡片头部。远程打开、下载和发附件都不限文件大小；桌面端这条预览也可以拖成自己的一块窗格。面板左侧的树列的是**这件事**引用过的全部路径——不是只有这一条消息的，也不是工作目录里的全部文件，相关指的是被引用过；树以工作目录为根，这件事在目录之外引用的路径和它并列平铺，本条消息自己引用的那几个标出来，面板与聊天共用一栏、中间拖条改宽度；同一条消息多个路径或打开工作区时左侧是文件树，树和内容之间可拖。拖过的预览栏宽度和文件树宽度，退出后再开还在。文本用 Monaco + Shiki 编辑（行号、查找 / 替换走 ⌘F、匹配括号·标签·标题折叠、换行）；预览自己不再放查找、换行、复制、保存、关闭这些按钮。Markdown 和单文件 HTML 默认看渲染结果，内容顶部正中有一个浮动按钮，不随正文滚动，在预览和源码之间切换。PDF 用内置的 pdf.js 渲染（翻页、缩放、适合宽度 / 整页、页内查找），不走系统的 PDF 查看器。区内 UTF-8 文本可 Cmd+S / 保存写回同一路径；未保存切文件或关面板先确认。桌面端预览没有自己的标题栏，文件名和关闭都在工作台标签上；窄屏和手机留一条返回加文件名。文件树每一项右键是「在 Finder 中打开」和「用系统默认应用打开」，本机窗口才生效。交接时把路径写进给对方的那条消息，不另建种类。种类按扩展名在预览时推导。区内的产物可以批注（见「批注」）。
_Avoid_: 资产库, Artifact 表, 快照副本, 每 Bot 产物目录, `@文件`

**批注（Annotation）**：
你对某个产物的某一处写的一句意见。挂在一个工作区相对路径、一个锚点和交出这个产物的那条消息上。先是草稿，Bot 看不见；攒够一批一起发出，合成一条引用交付消息的回复，并 @ 交出它的 Bot。发出后是「待处理」，Bot 或你把它标成「已处理」；已处理的可以重新打开。一标成已处理就从文件上撤下——图上的框、代码页边的点、Markdown 里的标记、PDF 页上的框、HTML 的编号、时间轴上的标记——留在文件上的就是还没处理的；列表里照旧列着。预览栏的「显示已处理」勾上才画回来，只对这块预览有效；从列表点一条已处理的去定位，那一条在定位期间画出来，再点一下取消选中就撤下来；对话里点它的卡片只打开列表停在那一条，不画回文件上。文件改了、锚点对不上时标「原文已变」，状态不变；文件不在了标「文件不在了」。批注不是产物的快照：只记下当时的引文、位置和文件哈希，不保存旧文件（选区长过引文上限时，另记整段的长度和指纹，用来判断这一段还在不在、变没变）。挂到谁：从某条消息打开的预览，就挂到那条消息和交出它的 Bot；交付消息在你不在的 Bot↔Bot 私聊里时，这批发到你和交付 Bot 的私聊里，消息上记着来源。只批工作区内的文件，链接类产物不能批注。批注消息叫醒的每个 Bot 沿用它自己交出产物那一轮的工作目录（这批里它最近的一次交付），哪怕会话已经静默够久、那件事已经关了——改的是同一件交付物；那件事属于别的会话（Bot↔Bot 私聊交出的）时，这一轮只是在那个目录里干活，不把那件事重新打开，两边会话各自接着的事都不受影响。它就是一条普通的后续发言：落在交付那一轮上、进纠正链，不加权。一批最多 50 条，一条正文最多 2000 字。群里一批可以同时交给几个 Bot：上下文里每条批注都写明交给了哪个 Bot（交出那个产物的 Bot，删了就写「已删除的 Bot」），`list_annotations` 也带上；每个 Bot 只处理交给自己的，交给别的 Bot 的留给那个 Bot，可以提一句；交给已删除的 Bot 的没人接，归这批叫醒的 Bot 处理。Bot 能做的：逐条处理交给自己的，改了的用 `resolve_annotation` 标成已处理并写一句怎么改的（任何 Bot 都可以标——Bot 不是权限边界——记下是谁标的），用 `list_annotations` 查还没处理完的（路径和 `read_file` 一样解析，工作区外的路径直接报错），`read_file` 读到还有待处理批注的文件时会被提醒。Bot 不能做的：重新打开、删除、新建批注。清空历史、删会话时挂在这些消息上的批注一起删掉；删了文件不删批注。锚点种类：文本 / 代码行区间、Markdown 渲染态划词、图片框选、PDF 页内区域、HTML 元素、音视频时间点。
_Avoid_: 评论（留给以后可能有的多人场景）, 审阅, 新的消息种类, 批注存旧版本, 每条批注单独叫醒一次, Bot 给你批注

**附件（Attachment）**：
聊天里**你**丢进来的文件，复制到工作区 `inbox/`，消息上保留指向该副本的链接。远程连接另有一条专门用来发文件的会话：发到那里的文件同样进 `inbox/`，也能发文字，文字只留在这条会话里；这条会话没有 Bot，不叫醒任何人。在普通会话里发的文件仍叫醒那条会话。Bot 把工作区里已有文件当产物挂上时也走同一套附件行，但不复制、不进 `inbox/`。工作区是电脑，附件不是第二套文件系统。栅格图进轮次窗时按像素发给模型，但一轮最多带 20 张、20 MB：从触发消息起、由新到旧取，放不下的旧图只留路径；大图先缩到长边 1568、不透明的转 JPEG 再发。其它文件只给路径，要读走 `read_file`。
_Avoid_: 只挂在消息上的幽灵文件

**人设（Profile）**：
Bot 的名字、职责说明、边界和头像。Bot 可改自己的名字、职责、边界和头像，改完不在转录插入人设变更条；你随时能再改。未删除名必须唯一。头像是生成图或一张栅格图，不是记忆。Bot 面板里的改动自动保存（停手片刻即写入，切换或关面板前把未保存的先发出去），没有保存和关闭按钮；面板只有右上角一个 ✕。设置里的工作区、端点和 MCP 名称 / 备注同样改完即写入；新增 MCP 或改 command / args / URL / headers 仍须确认，没有 Always allow。
_Avoid_: 把记忆写进人设, 人设面板的保存按钮, 设置里的保存按钮

**补跑（Catch-up）**：
守护进程回来之后（唤醒、崩溃拉起、退出后再开、开机），每个错过的日程只再执行最近该跑的那一次，不把错过的周期连开。睡眠醒来后先等一分钟再补跑：合盖时系统会自己醒几秒，那时网络常常还没回来。
_Avoid_: 错过就跳过, 错过几次跑几次, 只补睡眠不补退出

**上下文窗（Context window）**：
一轮里模型实际看到的：人设、启用中的技能目录、系统指令、本轮 MCP、这个 Bot 的记忆、局面块（群里有在场成员、活轮、谁叫醒、用户最近一句；每一轮都有这件事的规划要点（目标、验收、规则、流程与分工、进展）、任务清单、本轮任务、同类先例、已交出的文件、经过、自己待回看的那一次，还没整理出要点时退回最初的要求，第一轮标「这是这件事的第一轮」；全部从库里已有的行读出，轮次本身不叫模型摘要）、最近一段转录（含栅格附件的像素，只取最新的一份额度；带批注的用户消息在正文后面逐条展开每条批注的 id、交给哪个 Bot、路径、位置、引文或裁图和意见（页面和你写的文字折成一行或逐行缩进，冒充不了下一条），裁图和栅格附件花同一份额度、作为像素发给模型，额度用完的裁图只留批注的文字），以及本轮的工具回路。完整历史只存在库里，给你翻、给搜索，不整段塞进模型；判断调用用另一套更短的窗口，不含图。过大的工具结果先把完整 JSON 留在工作区，窗口里只放受限预览和 full_result_path；Bot 可继续用 shell 提取所需字段或解码文件，不把预览截断当作原始结果丢失。
_Avoid_: 自动摘要进人设, 语义检索记忆层, 后台蒸馏器, 按 token 估算, 把历史轮次的工具结果再喂进去

**花费（Spend）**：
本机只增不减的调用账本。七类端点调用各记一行：轮次补全、判断、开轮前选路、纠正链复盘、复盘后的每一跳学习、输入建议、整理。选路和整理属于决策路由；复盘与学习跳合为反馈路由；判断单列，输入建议归其他；输入建议和整理都不归任何 Bot。每行存实际模型、端点、思考等级及写入时的会话和 Bot 名称；删会话、清空历史、删 Bot 都保留，已删除对象用名称快照并标「已删除」，迁移时找不到模型的显示「未记录模型」。token 只用端点 `usage`，缺了显示「—」。供应商给出 `cost_in_usd_ticks` 时记实报；没有实报、输入与输出 token 齐全且配置了计费单价时算估算；估算跟着现在配置的单价走，设、改或清空某个模型的单价会重算它在该端点上所有没有实报的调用，从名单里去掉的模型保留原有估算。缓存输入使用缓存单价（留空沿用输入价，缓存 token 缺了当 0）；推理 token 已含在输出里，不重复加。实报与估算分开合计，覆盖不全会说明。信使有独立花费视图：桌面一个窗格标签，窄屏和手机一个浮层，可按时间、模型、会话、Bot 及调用类别筛选、看趋势与逐次明细；明细可跳回触发消息。会话顶栏、列表行、设置和模型选择记录都不放花费数字。时间范围和维度只保存在本机。
_Avoid_: 预算熔断, 日限额, 自动停机, 把估算当实报或混成一个金额, 用分词器估算缺失 token, 把判断并进轮次, MCP 工具自身账单

**本机接口（Local API）**：
守护进程在 `127.0.0.1` 上给窗口用的 HTTP + WebSocket，带本机 token。变更走 HTTP；转录流走一条只收的 WebSocket。窗口经它读写名册、发消息、订阅转录流。
_Avoid_: 把 UI 进程当运行时, 云 API, 双向命令通道, 工作区文件当 HTTP 资源, 把本机 token 放进 URL

**本机 token（Local token）**：
守护进程每次进程启动新铸的随机秘密，用来认证对本机接口的请求。HTTP 用 `Authorization: Bearer`；WebSocket 在连上后第一条消息带上，不进 URL。窗崩不换；守护进程退出或崩溃后下一进程再铸。不是端点 API key，不进 SQLite，不进仓库。
_Avoid_: API key, 安装一次的长活密钥, 写进 .env, 放进 WebSocket query

**判断日志（Judgement log）**：
当前对话设置里可见的下场 / 旁观记录。不进主转录。
_Avoid_: 把判断当轮次, 主转录刷屏

**模型选择记录（Model choice log）**：
一轮开轮时挑的模型和思考等级、这条消息算哪类、这轮怎么结束（补全失败带失败种类）、用了多久做了几跳，以及后来落在这一轮上的模型反馈、这条纠正链的复盘和记下的经验。它挂在流程图上那一轮的卡片上，不再有单独的入口和页面：Bot 卡片底下一行写着模型、思考等级和类别，有反馈、归咎模型、补全失败各有记号；点这一行在卡片下面展开全部，反馈点一下跳回原话。流程图工具条可以把有反馈或复盘归咎模型的卡片点亮、其余变暗，整件事的形状不变。按这件事看而不是按会话看：一件事跨了几条会话，它的选择都在同一张图上。不进主转录，也不展示 token 或花费；本机接口仍可逐会话列出。
_Avoid_: 路由日志, 单独的模型选择记录页, 会话顶栏上的记录按钮, 进主转录, 塞进当前对话设置, 在这里放花费统计, 把对内容的批评当模型反馈
